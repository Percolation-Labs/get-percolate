apiVersion: v2
name: percolate
description: An AI workflow engine in Postgres — DAG workflows, agents, graph and semantic query, all as rows
type: application
# The chart's own version. Bumped whenever templates or defaults change, which
# is not the same event as the application changing -- appVersion tracks that.
version: 0.1.3
appVersion: "0.1.7"
home: https://percolation-labs.github.io/get-percolate
icon: https://percolation-labs.github.io/get-percolate/assets/logo.png
sources:
  - https://github.com/Percolation-Labs/get-percolate
keywords: [postgres, workflow, agents, rag, pgvector, ai]
maintainers:
  - name: Percolation Labs
    url: https://percolationlabs.ai
